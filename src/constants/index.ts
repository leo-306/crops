import { CoordinateConfig } from '@/types';

export const BASE_COORDINATE_CONFIG: CoordinateConfig = {
	minWidth: 0,
	minHeight: 0
};

export const INIT_RECT = { x: 0, y: 0, width: 0, height: 0, left: 0, top: 0 };

export const INIT_IMG_RECT = { width: 0, height: 0 };